# testing_about_codex

Prototype repository for an emergency dispatch web interface.

## Structure

The project skeleton lives in `emergency-app/` and follows a modular layout for each step of the dispatch workflow.
